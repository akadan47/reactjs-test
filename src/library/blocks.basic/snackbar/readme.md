## Блок snackbar (snackbar)

#### Пример PUG вызова
```html
+snackbar(null, {id:'js-snackbar-addtobasket', timeout: 3000})

```

* Аргументы

1. `id` - id снэкбара 
2. `timeout` - время показа снекбара (необязательный)